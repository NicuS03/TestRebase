name: Test

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'
  

jobs:
  build-uboot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          path: something-file
      - name: Test Tag
        run: |
          echo ${{github.ref_name}}
    
