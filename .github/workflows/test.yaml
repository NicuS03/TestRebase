name: Test

env: 
  ENV_1: env_1
  ENV_2: env_2_${{env.ENV_1}}


on:
  workflow_dispatch:

jobs:
  build-uboot:
    runs-on: ubuntu_latest
    steps:
      - name: Clean-up artifacts
        run: |
          echo ${{env.ENV_2}}
